<mvc:View
    controllerName="sapui5test.controller.View1"
    xmlns:mvc="sap.ui.core.mvc"
    displayBlock="true"
    xmlns="sap.m"
>
	<Shell id="shell">
		<App id="app">
			<pages>
				<Page id="page" title="{i18n>title}">
					<content>
					<Title text="THIS IS A TITLE"></Title>
					<Button
						id="helloButton"
						type="Emphasized"
						icon="sap-icon://alert"
						text="SAPUI5"
						press="onShowHello"
						class="sapUiSmallMargin"/>
					
					
					</content>
						<TileContainer
			id="container"
			tileDelete="handleTileDelete"
			tiles="{/TileCollection}">
			<StandardTile
				icon="{icon}"
				type="{type}"
				number="{number}"
				numberUnit="{numberUnit}"
				title="{title}"
				info="{info}"
				infoState="{infoState}" />
		</TileContainer>
		<footer>
			<Toolbar>
				<ToolbarSpacer/>
					<Button text="Edit" press="handleEditPress" />
					<Button text="Busy state" press="handleBusyPress" />
				<ToolbarSpacer/>
			</Toolbar>
		</footer>
				</Page>
			</pages>
		</App>
	</Shell>
</mvc:View>
